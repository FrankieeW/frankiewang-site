---
export function getStaticPaths() {
  return [
    { params: { slug: "lean-study-kit" } },
    { params: { slug: "research-homebase" } },
    { params: { slug: "daily-proof" } },
  ];
}

const { slug } = Astro.params;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content={`0; url=/en/products/${slug}/`} />
    <link rel="canonical" href={`/en/products/${slug}/`} />
    <title>Project | Feng-Cheng Frankie Wang</title>
  </head>
  <body>
    <p><a href={`/en/products/${slug}/`}>Continue to English project</a></p>
  </body>
</html>
